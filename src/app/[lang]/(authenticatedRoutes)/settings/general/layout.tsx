import type { PropsWithChildren } from "react";

export const metadata = {
  title: "Izeat - General",
  description: "General settings page",
};

const GeneralSettingsLayout = ({ children }: PropsWithChildren) => {
  return <>{children}</>;
};

export default GeneralSettingsLayout;
